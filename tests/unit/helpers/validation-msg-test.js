import {
  validationMsg
} from '../../../helpers/validation-msg';
import { module, test } from 'qunit';

module('ValidationMsgHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = validationMsg(42);
  assert.ok(result);
});
